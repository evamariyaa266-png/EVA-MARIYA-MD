const fs = require('fs');
const path = require('path');

// Core functions for plugins
const loadPlugins = () => {
    // Basic plugin loader handler
    return true;
};

module.exports = {
    loadPlugins
};
